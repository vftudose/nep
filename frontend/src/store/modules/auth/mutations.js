export default {
    SET_STATUS: function (state, status)  {
        state.status = status
    },
    SET_TOKEN: function (state, token) {
        state.token = token;
    },
    SET_USER: function (state, user) {
        state.user = user;
    }
}
